[user]
	name = Matthieu Antoine
	email = matthieu@matthieu-antoine.me
	signingkey = D1CDC25385A36BC977B078F13DE12AB9A580A5DA
[commit]
	gpgsign = true
[core]
        pager = diff-so-fancy | less --tabs=4 -RFX
	editor = vim
[alias]
	logs = log --show-signature
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	pretty= log --pretty=format:"%h%x09%an%x09%ad%x09%s"

     	branch-name = "!git rev-parse --abbrev-ref HEAD"
        p = "!git push -u origin $(git branch-name)"
	last-commit="!git log --format="%H" -n 1"
	pf = p --force-with-lease
[gpg]
	program = gpg
[url "git@github.com:"]
	insteadOf = https://github.com/
